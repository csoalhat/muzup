<h1>Users index</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.email %>
      -
      <%= user.password_digest %>
      -
      <%= link_to('View User', profile_path(user.profile)) %>
    </li>
  <% end %>
</ul>